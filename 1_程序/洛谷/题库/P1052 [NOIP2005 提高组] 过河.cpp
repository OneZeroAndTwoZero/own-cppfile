#include<bits/stdc++.h>
using namespace std;

int l,s,t,m;
int a[1005];

bool check(int x){
	int pos = x,tem = 0;
	for(int i = 0;i < m;i ++){
		while(a[i])
	}
}

int main(){
	scanf("%d %d %d %d",&l,&s,&t,&m);
	for(int i = 0;i < m;i ++) scanf("%d",&a[i]);
	sort(a,a + m);

	return 0;
}

