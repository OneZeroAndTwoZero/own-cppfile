#include<bits/stdc++.h>
using namespace std;

string a[1005];
stack<string> ans;
int k = 0;

int main(){
	while(cin >> a[k]){
		k ++;
	}
	for(int i = 0;i < k;i++){
		if(a[i] == "+" || a[i] == "-" || a[i] == "*" || a[k] == "/")
	}

	return 0;
}

