#include<bits/stdc++.h>
using namespace std;

string a[105];

bool ok(string h1,string h2,string h3,string h4,int i){
	string s = s[i];
	int pos = 0;
	while(pos < s.size()){
		int min
		int bx = 0;
		if(s.find(h1,0) == s.npos) bx ++;
		else 
	}
}

int main(){
	int n;
	cin >> n;
	for(int i = 0;i < n;i++){
		cin >> a[i];
	}
	string h1,h2,h3 = "",h4 = "";
	cin >> h1 >> h2;
	for(int i = h1.size() - 1;i >= 0;i--){
		h3 = h3 + h1[i];
	}
	for(int i = h2.size() - 1;i >= 0;i--){
		h4 = h4 + h2[i];
	}

	return 0;
}

