#include<bits/stdc++.h>
using namespace std;

int t;

int main(){
#ifndef ONLINE_JUDGE
    freopen("../data.in","r",stdin);
    freopen("../data.out","w",stdout);
#endif
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    
    while(~scanf("%d",&t)){
        if(t == 0) break;
        printf("%d\n",t);
    }

    return 0;
}
